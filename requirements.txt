# Schicht 3: API-Latenz-Messungen
httpx[http2]>=0.27
websockets>=13.0
python-dotenv>=1.0
certifi>=2024.0

# Analyse (Notebooks 00-07)
pandas>=2.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
seaborn>=0.13
pyarrow>=15.0
jupyter>=1.0
nbconvert>=7.0

# Layer-2 PCAP-Analyse (NB 02, lokal ohne tshark)
scapy>=2.5          # IP/Paket-Extraktion aus PCAP
dnspython>=2.4      # ASN-Lookup via Team-Cymru DNS

# Cloud-Steuerung (EC2-Validierung/Captures)
boto3>=1.34
