# sample.wav

Liegt in `measurements/layer3/sample.wav` (bereits erstellt).

- 16 kHz, Mono, 16-bit PCM, ~4.8s
- Text: "Good morning. I would like to know the current weather forecast for Frankfurt."
- Validierung: Deepgram-Testlauf mit `transcript_len > 0` steht noch aus
